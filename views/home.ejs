<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather Forecast</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-100 via-blue-200 to-blue-300 min-h-screen">

  <%-include('../partials/nav.ejs')%>

  <div class="flex flex-col items-center mt-20">
    <div class="bg-white/80 backdrop-blur-md rounded-2xl shadow-2xl px-10 py-10 w-full max-w-lg flex flex-col items-center">
      <h1 class="text-5xl font-extrabold mb-8 text-blue-700 drop-shadow-lg tracking-tight">Weather Forecast</h1>

      <% if(login){ %>
      <form action="/getweather" method="GET" class="mb-10 w-full">
        <div class="flex shadow-lg rounded-lg overflow-hidden">
          <input 
            type="text" 
            name="city" 
            placeholder="Enter city name" 
            class="w-full px-5 py-3 text-lg border-0 focus:outline-none focus:ring-2 focus:ring-blue-400 bg-blue-50 placeholder-gray-400"
            autocomplete="off"
          >
          <button 
            type="submit" 
            class="bg-gradient-to-r from-blue-500 to-blue-700 text-white px-8 py-3 text-lg font-semibold hover:from-blue-600 hover:to-blue-800 transition duration-200"
          >
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 inline-block mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-4.35-4.35m0 0A7.5 7.5 0 104.5 4.5a7.5 7.5 0 0012.15 12.15z" />
            </svg>
            Search
          </button>
        </div>
      </form>

      <% if(obj && obj.city){ %>
      <div class="bg-gradient-to-br from-blue-200 via-white to-blue-100 p-8 rounded-2xl shadow-xl w-full text-center border border-blue-100 hover:scale-105 transition-all duration-300">
        <div class="flex flex-col items-center mb-4">
          <div class="flex items-center justify-center w-16 h-16 rounded-full bg-blue-100 shadow-inner mb-2">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 15a4 4 0 004 4h10a4 4 0 100-8h-1.26A8 8 0 103 15z" />
            </svg>
          </div>
          <span class="text-xs text-blue-400 font-semibold tracking-widest">Weather ID: <%=obj.id%></span>
        </div>
        <h2 class=" text-3xl font-bold text-blue-700 mb-1 animate-pulse"><%=obj.city%></h2>
        <p class="text-lg text-blue-500 mb-4 font-medium">Country: <span class="font-semibold text-blue-700"><%=obj.country%></span></p>
        <div class="flex justify-center gap-8 mb-2">
          <div class="flex flex-col items-center">
            <span class="text-2xl font-bold text-blue-600"><%=obj.temp%>°C</span>
            <span class="text-sm text-gray-500">Temperature</span>
          </div>
          <div class="flex flex-col items-center">
            <span class="text-2xl font-bold text-blue-600"><%=obj.humid%>%</span>
            <span class="text-sm text-gray-500">Humidity</span>
          </div>
          
        </div>
        <div>
          <form class="bg-gray-400" action="/fav" method="get">
            <input type="hidden" name="favCity" id="fav" value="<%=obj.city%>">
            <input type="submit" value="favorite">
          </form>
        </div>
      </div>
      <% } %>
      <% } else { %>
        <div class="mt-8 text-center text-lg text-gray-600">
          <span class="inline-block bg-blue-100 text-blue-600 px-4 py-2 rounded-lg shadow">Please log in to search for weather information.</span>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
